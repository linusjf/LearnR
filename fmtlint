#!/usr/bin/env bash

USAGE="Usage: $0 dir1 dir2 dir3 ... dirN"

if [ "$#" == "0" ]; then
	echo "$USAGE"
	exit 1
fi

echo "$@"
./style.R "$@" | tee styler.out && 
  (./lint.R "$@" | tee linter.out)
exit "$?"
